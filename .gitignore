# original prototype kept locally for reference, not published
gemini-code-*.html

# working notes, already folded into content.js
Faiths.txt
Factions.txt
Updated cohorts.txt
*.docx

# source scripts for episodes.js (root folder only; images/episodes/ must deploy)
/episodes/

# unused image variants parked for eventual deletion
backup-images/

# chatbot source (deployed copy lives in Cloudflare; keeps system prompt out of the public repo)
ChatFiles/

# game prototype, not ready to publish
game/
